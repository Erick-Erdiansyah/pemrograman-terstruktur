program deret_angka;
uses crt;
var karakter : char;
    baris,i,j,kolom : integer;
begin
clrscr;
for i := 1 to 4 do
begin
  for j := 1 to 9 do
  begin
    write(j);
  end;
  write(0);
  writeln;
  end;
readln;
end.